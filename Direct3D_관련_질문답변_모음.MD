# Direct3D 관련 질문답변 모음

## Vertices

1. 점의 연결순서를 바꾸면, 뒤집히는 이유는 무엇인가?\
A: 정점의 순서를 이용하여 앞면/뒷면을 판단하기 때문이다. 정점의 순서가 달라진 경우 외적의 결과가 반대가 되어 삼각형의 법선 방향이 바뀐다.

## Matrices

1. 컬링은 무엇인가? 왜 해야하는가?\
A: 컬링은 카메라에서 보이지 않는 면을 렌더링하지 않는 것이다. 컬링을 하면 불필요한 렌더링 작업을 줄여 성능 최적화를 할 수 있다.

2. 월드, 뷰, 프로젝션 행렬은 무엇인가?\
World Matrix: 물체의 위치/회전/크기를 나타냄.\
View Matrix: 카메라의 위치/방향을 나타냄.\
Projection Matrix: 3D를 2D 화면으로 투영함.\
세 행렬을 World -> View -> Projection 순서로 적용하여 정점이 화면에 그려진다.

3. 공간변환은 어떻게 이루어지는가?\
A: 행렬과 벡터의 곱으로 이루어진다.

## Lights

1. Geometry란 무엇인가?\
A: 3D 물체의 모양을 표현하기 위한 기하 데이터

2. 깊이버퍼에 대해서 설명해보자.\
A: 3D 공간에서 여러 물체가 서로 겹쳐 있을 때 어떤 물체가 카메라에 더 가까운지를 판단하기 위한 버퍼이다. 깊이 버퍼에는 각 픽셀에 대한 깊이 값이 저장된다. 그래서 물체의 그리는 순서와 관계없이 제대로 보인다.

3. 디퓨즈와 엠비언트는 의미가 무엇인가?\
Diffuse: 확산광, 빛이 물체 표면에 직접 들어오면 표면의 방향에 따라 밝기가 달라진다. 법선 벡터와 빛의 방향을 내적하여 계산한다.\
Ambient: 주변광, 빛을 직접 받지 않아도 반사 등에 의하여 어느 정도의 빛을 받는 것이다. 주변광은 방향이 없어서 물체의 어느 방향을 보고 있더라도 적용된다.

## Meshes

1. 3D모델데이터는 어떻게 제작되는가?\
A: 3D 모델 데이터는 3차원 공간에 점과 면을 정의하고, 그 표면에 필요한 추가 정보를 붙여 만든 데이터이다. 여러 삼각형을 연결하면 Mesh가 되고 Index가 정점의 연결 관계를 정의한다.\
이 후 UV Unwrap을 통해 3D 표면을 2D로 펼친다. 이 UV에 맞춰 텍스처를 제작한다.

## Billboard

1. 빌보드는 무엇이며 어떤 용도로 사용되는가?\
A: 빌보드는 3D 공간에 있는 물체가 항상 카메라를 바라보도록 회전시키는 기법이다. 복잡한 3D 모델을 사용하지 않고 2D 이미지로 3D 공간의 물체를 표현하기 위해 사용된다.

2. 파티클이펙트와 빌보드를 이용한 이펙트의 차이점은 무엇인가?\
A: 파티클이펙트는 동적인 효과를 구현하는 방식으로 구현하는 방법 중 하나가 빌보드이다.

## Terrain

1. 유니티에 Terrain의 높낮이를 바꾸는 것은 어떻게 가능한가?\
A: Heightmap(높이맵)을 기반으로 지형의 높이를 관리하기 때문이다.

2. 프러스텀 컬링을 수행하는 이유는 무엇인가?\
A: 카메라가 현재 볼 수 없는 3D 오브젝트를 렌더링하지 않아서 성능을 향상시키기 위해서이다.

3. 쿼드트리를 이용하는 이유는 무엇인가?\
A: 넓은 2차원 공간에 존재하는 많은 객체를 공간적으로 분할하여, 필요한 객체만 빠르게 찾기 위해서이다. 즉, 프러스텀 컬링을 효율적으로 수행하기 위한 공간 검색 구조로 사용한다.

4. LOD(Level of Detail)는 무엇인가 왜 수행해야하는가?\
A: 카메라와 물체 사이의 거리에 따라 서로 다른 복잡도의 모델을 사용하는 기법이다. 즉 가까이 있는 물체는 정밀하게, 멀리 있는 물체는 단순하게 렌더링하는 것이다. 렌더링 비용을 줄이기 위해서 필요하다.