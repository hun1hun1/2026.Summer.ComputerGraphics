# 용어정리

## 자료구조 및 객체 지향 프로그래밍

컨테이너: 데이터를 담는 변수\
벡터: 동적배열\
이터레이터(Iterator): 주소값의 시작점을 가리킴.

변수(속성): 데이터를 저장하기 위한 이름이 붙은 메모리 공간\
ㄴ속성: 객체가 가지고 있는 데이터를 표현할 때 사용하는 개념\
함수(동작,이벤트): 특정 작업이나 동작을 수행하기 위해 작성한 코드의 묶음\
ㄴ동작: 객체가 수행하는 함수\
ㄴ이벤트: 특정 사건이 발생했을 때 실행을 시작하는 진입점

메세지: 한 객체나 시스템이 다른 객체나 시스템에게 특정 동작이나 정보를 전달하기 위한 데이터\
콜백구조: 어떤 함수를 다른 함수나 시스템에 전달해 두었다가, 특정 사건이나 조건이 발생했을 때 해당 함수를 다시 호출하는 구조

가상함수: 부모 타입으로 여러 자식 객체를 관리하면서 각각 다른 동작을 수행할 수 있게 하는 함수\
함수포인터: 함수의 주소값을 저장하는 변수\
가상함수포인터: 객체가 자신의 가상함수테이블을 가리키기 위해 가지는 숨겨진 포인터

가상함수테이블: 가상 함수들의 주소를 저장하는 테이블\
상속: 기존 클래스의 속성과 동작을 새로운 클래스가 물려받아 사용하는 기능\
다형성: 서로 다른 객체가 각자의 방식으로 동작 할 수 있는 특성

핸들러: 특정 이벤트, 메시지, 요청 또는 상황이 발생했을 때 이를 처리하는 코드나 함수를 의미함.

## 운영체제(Win API / MFC)

[소프트웨어]\
API(Application Programming Interface): 서로 다른 프로그램이나 시스템이 정해진 규칙에 따라 기능이나 데이터를 주고받을 수 있도록 만들어 놓은 인터페이스\
Interface: 면에 접촉해서 어떤 장치를 작동시키는 것.\
SDK(Software Development Kit): 특정 플랫폼, 운영체제, 서비스 또는 기술을 대상으로 소프트웨어를 개발할 수 있도록 제공되는 개발 도구 모음임.\
library: 함수들의 집합

---
[하드웨어]\
ARM: 스냅드래곤, 엑시노스...\
x86(x64): 인텔, amd...

운영체제의 비트수: 2<sup>n</sup> (x86 / 2<sup>32</sup>, x64 / 2<sup>64</sup>)

APU: GPU와 CPU가 합쳐진 형태, 내장그래픽 메모리를 공유하여, 메모리의 복사량을 줄일수 있음.\
CPU: 컴퓨터의 명령어를 해석하고 실행하는 핵심 하드웨어\
ㄴ직렬처리: 여러 작업을 한 번에 하나씩 순차적으로 처리하는 방식\
GPU: 그래픽 연산과 대규모 병렬 연산에 특화된 프로세서\
ㄴ병렬처리: 하나의 작업을 여러 개의 작은 작업으로 나누어 여러 연산 유닛에서 동시에 처리하는 방식

---
프로세스: 실행 중인 프로그램\
스레드: 실제 CPU에서 작업을 수행하는 실행 흐름\
테스크: 컴퓨터에서 수행해야 하는 하나의 작업 단위\
잡스케쥴러: 어떤 작업을 언제 어떤 실행 자원에서 처리할지 결정하는 시스템, 여러 CPU 코어를 효율적으로 사용하기 위해 작업을 분배하는 역할\
크리티컬섹션: 여러 스레드가 동시에 접근할 수 있는 공유 자원에 접근하는 코드 영역, 한 번에 하나의 스레드만 접근하도록 제한함.\
컨텍스트스위칭: CPU가 현재 실행 중인 프로세스나 스레드의 실행 상태를 저장하고, 다른 프로세스나 스레드의 실행 상태를 복원하여 실행을 전환하는 작업\
오버헤드: 실제 작업을 수행하기 위해 필요한 부가적인 처리 비용

마스크: 특정 영역만 선택하거나, 특정 영역의 처리 여부를 제어하기 위한 데이터

## Direct2D

랜더타겟: 그리기 명령의 결과가 실제로 출력될 대상\
행렬: 위치, 회전, 크기, 좌표계 등을 변환하기 위해 사용하는 수학적 도구

## Direct3D(컴퓨터 그래픽스)

랜더링파이프라인: 로컬->월드->뷰->프로젝션->레스터라이즈\
버택스: 3차원 공간에 존재하는 하나의 점을 나타내는 데이터\
버텍스버퍼: GPU에서 렌더링할 정점(Vertex) 데이터를 저장하는 메모리 공간\
인덱스버퍼: 버텍스 버퍼에 저장된 정점들을 어떤 순서로 사용할 것인지 지정하는 데이터\
컬링: 화면에 그릴 필요가 없는 객체나 폴리곤을 렌더링 과정에서 미리 제외하여 불필요한 연산을 줄이는 최적화 기법\
더블버퍼링: 두 개의 버퍼를 번갈아 사용하여 화면이 깜빡이거나 그려지는 과정이 보이는 현상을 방지하는 기법\
삼중버퍼링: 렌더링에 3개의 버퍼를 사용하는 방식, Front Buffer + Back Buffer 2개\
ㄴ스텐실버퍼: 뷰에서 보는 z축의 좌표가 저장된 버퍼\
지오메트리: 3D 공간에 존재하는 물체의 형태와 구조를 정의하는 데이터를 의미\
확산광: 빛이 표면을 얼마나 정면으로 비추고 있는가에 따라 밝기가 달라지는 것\
주변광: 특정한 방향이나 위치에 의존하지 않고 장면 전체에 일정하게 적용되는 빛의 성분\
깊이버퍼: 3D 장면에서 각 픽셀이 카메라로부터 얼마나 가까운지를 저장하여, 서로 겹치는 물체 중 어떤 표면을 화면에 표시할지 결정하는 버퍼\
메쉬: 3D 공간에서 물체의 형태와 표면을 표현하기 위해 여러 개의 정점, 모서리, 면을 연결하여 구성한 데이터\
텍스처: 3D 모델의 표면에 색상이나 패턴, 세부 정보를 표현하기 위해 사용하는 2차원 이미지 데이터\
UV: 텍스처의 어느 위치를 사용할 것인지 나타내는 좌표, 일반적으로 0 ~ 1 범위로 정규화해서 사용함.\
Opaque: 뒤에 있는 물체가 보이지 않는 표면 또는 픽셀\
빌보드: 3D 공간에 배치된 2D 이미지 또는 평면이 항상 카메라를 바라보도록 회전시키는 렌더링 기법\
파티클: 작은 입자를 다수 생성하고, 각각의 위치, 속도, 크기, 회전, 수명, 색상, 투명도 등을 시간에 따라 변화시켜 시각 효과를 표현하는 기법\
높이맵: 이미지의 밝기 값 또는 특정 채널의 값을 높이 정보로 사용하여 3D 표면의 높낮이를 표현하는 텍스처\
라이트맵: 3D 장면의 조명 정보를 미리 계산하여 텍스처에 저장하고, 이를 렌더링할 때 사용하는 기법\
라이트베이킹: 라이트맵을 생성하는 과정\
절두체컬링: 카메라가 볼 수 있는 영역인 시야 절두체(View Frustum) 밖에 있는 객체를 렌더링에서 제외하는 최적화 기법\
오큘루전컬링: 카메라의 시야 안에 있지만 다른 물체에 가려져 있는 물체를 렌더링에서 제외하는 최적화 기법\
쿼드트리: 2차원 공간을 재귀적으로 4개의 영역으로 분할하여 객체의 위치를 효율적으로 관리하는 공간 분할 자료구조\
옥트리: 3차원 공간을 재귀적으로 최대 8개의 영역으로 분할하여 객체의 위치를 효율적으로 관리하는 공간 분할 자료구조\
LOD: 카메라와의 거리나 화면에서 차지하는 크기 등에 따라 3D 객체의 모델 복잡도를 단계적으로 변경하는 최적화 기법

프로그래머블파이프라인: 그래픽 렌더링 파이프라인의 일부 단계를 개발자가 쉐이더 프로그램으로 직접 작성하고 원하는 방식으로 동작을 정의할 수 있도록 만든 렌더링 방식\
쉐이더: GPU에서 실행되는 프로그램, 특정 단계에서 입력 데이터를 받아 계산하고 결과를 출력함.\
버택스쉐이더: 버텍스 단위로 실행되는 쉐이더, 버텍스의 좌표 변환\
픽셀쉐이더: 레스터라이즈 이후 생성된 Fragment/Pixel 후보에 대해 색상 등의 값을 계산\
헐쉐이더: 테셀레이션 단계에서 사용되는 쉐이더, 얼마나 세밀하게 분할할지 결정하고 테셀레이션에 필요한 제어 데이터를 생성하는 쉐이더\
도메인쉐이더: 테셀레이터가 생성한 새로운 버텍스의 위치와 데이터를 계산하는 역할\
지오메트리쉐이더: 하나의 Primitive를 입력받아 수정하거나, 삭제하거나, 새로운 Primitive를 생성할 수 있는 쉐이더\
테셀레이션: 하나의 큰 Primitive를 더 작은 Primitive들로 세분화하는 과정\
노말맵: 3D 모델의 표면에 미세한 요철이나 방향 정보를 표현하기 위해 각 픽셀마다 표면의 법선 벡터를 저장한 텍스처
